<view class="container">
  <view class="header">
    <view class="tabs">
      <view 
        class="tab-item {{currentTab === index ? 'active' : ''}}" 
        wx:for="{{tabs}}" 
        wx:key="index"
        data-index="{{index}}"
        bindtap="onTabChange"
      >
        {{item}}
      </view>
    </view>
    <view class="debug-btn" bindtap="goToAdmin">调试</view>
  </view>

  <view class="sub-tabs" hidden>
    <view 
      class="sub-item {{currentSub === index ? 'active' : ''}}" 
      wx:for="{{subCategories}}" 
      wx:key="index"
      data-index="{{index}}"
      bindtap="onSubChange"
    >
      {{item}}
    </view>
  </view>

  <view class="list-container">
    <block wx:if="{{!loading || list.length > 0}}">
      <view class="movie-list">
        <view 
          class="movie-item" 
          wx:for="{{list}}" 
          wx:key="_id"
          data-id="{{item._id}}"
          bindtap="goToDetail"
        >
          <view class="rank">{{index + 1}}</view>
          <view class="poster-wrap">
            <image 
              wx:if="{{item.poster}}" 
              class="poster" 
              src="{{item.poster}}" 
              mode="aspectFill" 
              lazy-load 
              binderror="onPosterError" 
              data-index="{{index}}" 
            />
            <view wx:else class="poster-placeholder">
              <text>{{item.title[0]}}</text>
            </view>
          </view>
          <view class="info">
            <view class="title-row">
              <text class="title">{{item.title}}</text>
              <text class="rating">★ {{item.rating}}</text>
            </view>
            <view class="meta">
              <text>{{item.year}}</text>
              <text wx:if="{{item.episodes}}">{{item.episodes}}集</text>
              <text>{{item.region === 'cn' ? '中国' : item.region === 'kr' ? '韩国' : item.region === 'jp' ? '日本' : item.region === 'us' ? '欧美' : ''}}</text>
            </view>
            <view class="tags">
              <text class="tag" wx:for="{{item.genres}}" wx:for-item="genre" wx:key="*this">{{genre}}</text>
            </view>
          </view>
        </view>
      </view>
      
      <view class="loading-more" wx:if="{{loading}}">
        <text class="loading-text">服务器摸鱼中</text>
        <image class="emoji-bounce" src="/images/loading.png" mode="aspectFit" />
        <image class="emoji-bounce delay-1" src="/images/loading.png" mode="aspectFit" />
        <image class="emoji-bounce delay-2" src="/images/loading.png" mode="aspectFit" />
      </view>
      
      <view class="no-more" wx:if="{{!hasMore && list.length > 0}}">
        <text>没有更多了</text>
      </view>
    </block>

    <view class="empty" wx:if="{{!loading && list.length === 0}}">
      <text>暂无数据</text>
    </view>

    <view class="loading-wrap" wx:if="{{loading && list.length === 0}}">
      <text class="loading-text">服务器摸鱼中</text>
      <image class="emoji-bounce" src="/images/loading.png" mode="aspectFit" />
      <image class="emoji-bounce delay-1" src="/images/loading.png" mode="aspectFit" />
      <image class="emoji-bounce delay-2" src="/images/loading.png" mode="aspectFit" />
    </view>
  </view>
</view>
