# 数据增量更新功能 - 检查清单

## 开发前检查

- [x] 已阅读并理解 spec.md 规范文档
- [x] 已备份现有 data.json 数据
- [x] 已确认豆瓣 API 可正常访问

---

## 代码实现检查

### 公共模块 (scripts/lib/incremental.js)

- [x] `loadExistingData()` 正确处理文件不存在的情况
- [x] `buildIndex()` 生成的索引结构正确
- [x] `compareWithExisting()` 正确识别新增/已存在/移除数据
- [x] `mergeData()` 正确合并新旧数据
- [x] `saveData()` 同时保存数据和索引

### 综艺脚本 (scripts/fetch_variety.js)

- [x] 增量更新逻辑正确
- [x] 国产综艺过滤逻辑保持不变
- [x] 分类逻辑（真人秀/喜剧/音综）保持不变
- [x] 热力值计算正确
- [x] 日志输出清晰
- [x] 第7步从索引构建列表时对所有条目重新计算 subCategory（修复旧条目分类过期问题）

### 电影脚本 (scripts/fetch_movie.js)

- [x] 增量更新逻辑正确
- [x] 地区分类逻辑保持不变
- [x] 热力值计算正确
- [x] 日志输出清晰

### 热剧脚本 (scripts/fetch_drama.js)

- [x] 增量更新逻辑正确
- [x] 分类逻辑（国产剧/韩剧/日剧）保持不变
- [x] 热力值计算正确
- [x] 日志输出清晰

---

## 功能测试检查

### 首次运行测试

- [x] 无索引文件时能正常运行
- [x] 自动生成索引文件
- [x] 数据格式正确

### 增量更新测试

- [x] 正确识别新增数据
- [x] 正确跳过已存在数据的详情获取
- [x] 已存在数据的热力值正确更新
- [x] 请求次数明显减少

### 全量更新测试 (--full)

- [x] 忽略索引，获取所有详情
- [x] 数据完整正确

### 热力值更新测试 (--hot-only)

- [ ] 只更新热力值，不获取详情
- [ ] 排序正确

---

## 兼容性检查

### 数据格式兼容

- [x] data.json 格式向后兼容
- [x] 索引字段不影响现有读取逻辑
- [x] server.js 能正常读取数据

### 小程序端兼容

- [x] API 返回数据格式不变
- [x] 小程序显示正常

---

## 性能检查

- [x] 增量更新请求次数减少 70% 以上
- [x] 更新时间明显缩短
- [x] 内存使用无明显增加

---

## 文档检查

- [x] README 已更新
- [x] 命令行参数说明清晰
- [x] 代码注释充分

---

## 阶段五检查：并发抓取与防封

- [x] `parallelLimit` 函数正确限制并发数
- [x] worker 间有随机延迟
- [x] IP 封禁检测正确
- [x] 指数退避策略生效
- [x] 增量更新中并发抓取稳定运行

---

## 阶段六检查：作品简介功能

### 简介数据抓取

- [x] `fetchSubjectSummary` 从移动版 HTML 正确提取简介
- [x] meta description 提取优先
- [x] subject-intro section 作为备选
- [x] 无简介时返回空字符串
- [x] HTML 长度异常时返回空字符串

### 弹窗 UI

- [x] 点击条目弹出卡片
- [x] 显示标题和简介
- [x] 点击空白关闭
- [x] 无简介时显示"暂无简介"

### 数据流

- [x] 索引中 abstract 字段存储简介
- [x] server.js 正确返回 summary 字段
- [x] 小程序正确显示简介内容

### 非电影过滤

- [x] 开幕式等非电影内容被正确过滤
- [x] 真正的电影不受影响

---

## 上线前检查

- [x] 所有测试通过
- [x] 代码已提交
- [ ] 服务器已拉取最新代码
- [ ] pm2 已重启服务
- [ ] 线上验证正常
